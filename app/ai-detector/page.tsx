"use client";
import Link from "next/link"; import {useState} from "react";
export default function Detector(){const [text,setText]=useState("");const [result,setResult]=useState<number|null>(null);return <><header><div className="wrap nav"><Link href="/" className="logo">Vix<span>rola</span></Link><div className="actions"><Link className="btn" href="/tools">All tools</Link></div></div></header><main className="toolPage"><div className="wrap toolShell"><div className="toolTop"><div className="eyebrow">AI Writing Tool</div><h1>AI Detector</h1><p>Paste writing to preview the detector workflow. A production detector can be connected to your selected AI provider later.</p></div><div className="panel"><div className="panelHeader"><span>Text to analyze</span><span className="status">{text.length} characters</span></div><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Paste text here..." /><button className="btn primary" onClick={()=>setResult(text.trim()?42:null)}>Analyze text</button>{result!==null&&<div className="notice">Preview result: {result}% AI-pattern signal. This demo value is not a real detection result.</div>}</div></div></main></>}